# Instructions:
# In Project folder:
# > touch Podfile
# Then:
# < pod install

# If cocoaPods fail due to old version
# [sudo] gem update cocoapods

platform :ios, :deployment_target => '6.0'

pod 'OCMock', '~> 2.1.1'